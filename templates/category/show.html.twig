{% extends 'base.html.twig' %}

{% block title %}{{ product.name }}- Amanie Cosmetics{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="row">
        <div class="col-md-5">
            <img src="{{ asset('assets/pictures/' ~ category.illustration) }}" height="150px" class="img-fluid" alt="{{ product.name }}">
        </div>
        <div class="col-md-7 my-auto">
        <h3> {{ category.name }} </h3>
        <p>{{ category.description}}</p>
      
    </div>
    </div>
          
    </div>    

{% endblock %}