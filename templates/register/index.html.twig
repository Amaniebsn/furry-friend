{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
   {% if notification %}
     <div class="alert alert-info">{{ notification }}</div>
{% endif %}
<hr>
{{form(form)}}
{% endblock %}
